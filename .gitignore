# data (kept local; large, not committed)
data/raw/*.csv
data/curated/

# python
backend/.venv/
etl/.venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example

# node / next.js
frontend/node_modules/
frontend/.next/
frontend/out/

# terraform
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*
infra/*.tfvars
!infra/*.tfvars.example
infra/tfplan*.out
infra/.env.secrets
# .terraform.lock.hcl is intentionally NOT ignored -- Terraform recommends committing
# it so local and CI always resolve the same provider versions.

# os / editor
.DS_Store
.vscode/
.claude/settings.local.json
