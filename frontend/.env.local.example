NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional. Leave unset for local dev -- Sentry.init() treats an empty DSN as a
# no-op. Get the real value from your Sentry Next.js project's Settings -> Client Keys (DSN).
NEXT_PUBLIC_SENTRY_DSN=
